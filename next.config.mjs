/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Site",
    output: "export", 
    reactStrictMode: true,
    images: { unoptimized: true },
};

export default nextConfig;
